$ceil(2.2)
$floor(2.6)
$round(2.6, 4)
$rnd(10, 5) // argument order doesn't matter, the larger number will be the max and the smaller will be the min
// 1st argument is also optional, if not provided, it will default to 0. The max allowed number is i128::MAX